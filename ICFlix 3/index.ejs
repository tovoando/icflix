<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
</head>
<body>
  May Node and Express be with you.
<ul class="contato">
<% var c = contato.length %>
<% for(var i = c - 10; i<contato.length; i++) {%>
  <li class="quote">
    <span><%= contato[i].nome %></span>
    <span><%= contato[i].fotocapa %></span>
    <span><%= contato[i].sinopse %></span>
  </li>
<% } %>
</ul>
</body>
</html>
